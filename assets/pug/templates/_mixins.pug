mixin icon(name,mod)
    - mod = mod || ''
    svg(class="icon icon-" + name + ' ' + mod)
        use(xlink:href="i/sprite/sprite.svg#" + name)

mixin navItem(iconId, text, mod="", hasArrow=true, link="#")
    - mod = mod || ''
    li.nav_menu__item
        a.nav_menu__link.jsMenu(class=mod, href=link)
            .nav_menu__content 
                +icon(iconId)
                span.nav_menu__span= text
            if hasArrow
                +icon("arrow")